language: android
sudo: required
jdk: oraclejdk8

before_cache:
 - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
 - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:
 directories:
 - $HOME/.gradle/caches/
 - $HOME/.gradle/wrapper/

env:
 global:
 - ANDROID_API=23
 - EMULATOR_API=23
 - ANDROID_BUILD_TOOLS=25.0.3
 - ANDROID_ABI=armeabi-v7a
 - ANDROID_TAG=google_apis
 - ADB_INSTALL_TIMEOUT=5 # minutes

# safelist
branches:
 only:
 - master
 - stable

android:
 components:
 - tools
 - platform-tools
 - tools
 - build-tools-$ANDROID_BUILD_TOOLS
 - android-$ANDROID_API
 - android-$EMULATOR_API
 - extra-google-m2repository
# - extra-android-m2repository # for design library
# - addon-google_apis-google-19 # google play services
#not needed while connectedCheck not runs
# - sys-img-armeabi-v7a-google_apis-$ANDROID_API
# - sys-img-armeabi-v7a-google_apis-$EMULATOR_API

 licenses:
 - android-sdk-preview-license-.+
 - android-sdk-license-.+
 - google-gdk-license-.+
 - '.+'
 
before_install:
#constraint layout
#- mkdir "$ANDROID_HOME/licenses" || true
#- echo -e "\n8933bad161af4178b1185d1a37fbf41ea5269c55" > "$ANDROID_HOME/licenses/android-sdk-license"
#- echo -e "\n84831b9409646a918e30573bab4c9c91346d8abd" > "$ANDROID_HOME/licenses/android-sdk-preview-license"
- chmod +x gradlew
#- ./gradlew dependencies || true # DON'T ADD unless you are getting "Install missing components using SDK manager"
#Source: https://medium.com/@oldergod/constraint-layout-and-circleci-travis-d50342696d2

install:
 - sdkmanager --list || true
 # Update sdk tools to latest version and install/update components
 - echo yes | sdkmanager "tools"
 - echo yes | sdkmanager "platforms;android-23" # Latest platform required by SDK
 - sdkmanager --list || true

#not needed while connectedCheck not runs
#before_script:
#- echo no | android create avd --force -n test -t android-23 --abi $ANDROID_ABI --tag $ANDROID_TAG
#- emulator -avd test -no-skin -no-audio -no-window &
#- android-wait-for-emulator
#- adb shell input keyevent 82 &

script:
- "./gradlew clean build test -PdisablePreDex --stacktrace"
#frozen 10m - "./gradlew clean build test connectedCheck -PdisablePreDex --stacktrace"
